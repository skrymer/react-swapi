import * as React from 'react';
import { Header } from 'semantic-ui-react';

const Home: React.FC = () => {
    return (
        <div style={{ marginTop: '7em' }}>
            <Header as="h1">Welcome to React SWAPI 0.1</Header>
        </div>
    );
};

export default Home;